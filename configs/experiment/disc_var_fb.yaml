# @package _global_
# Variational actions v2: free bits (4 nats) against posterior collapse
# (v1: KL -> 0.0025, codes uninformative, planner 0.13).
run_name: disc_var_fb
model:
  chunk_target: frozen
  value_detach: false
  variational_actions: true
objective:
  rollout: {weight: 0.0}
  hierarchy: {weight: 0.0}
  delta_action: {weight: 0.0}
  chunk_pred: {weight: 2.0}
  action_kl: {weight: 1.0, free_nats: 4.0}
  act_decode: {weight: 1.0}
