# @package _global_
# Next phase: trunk (FiLM/AdaLN-style) action conditioning on core5 —
# easy-domain control for the conditioning-mechanism question.
run_name: disc_core5_film
model:
  chunk_target: frozen
  value_detach: false
  predictor_kind: film
objective:
  rollout: {weight: 0.0}
  hierarchy: {weight: 0.0}
  chunk_pred: {weight: 2.0}
  value: {weight: 0.0}
  ranking: {weight: 1.0}
data:
  n_alt: 2
