# @package _global_
# Deprecated compatibility name. The implementation is strictly nested and
# has no separate static alignment objective.
defaults:
  - hierarchical_language_nested_sentence
  - _self_
