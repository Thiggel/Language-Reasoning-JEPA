# @package _global_
defaults:
  - hierarchical_language_macro_action_euclidean
  - _self_

model:
  config:
    enable_value: true

research:
  stage: VALUE_DISTILLATION
  require_admission_stage: ORACLE_HIGH_LEVEL
  terminal_used_offline_only: true

loss:
  token_dynamics: 0.0
  sentence_dynamics: 0.0
  variance: 0.0
  covariance: 0.0
  macro: 0.0
