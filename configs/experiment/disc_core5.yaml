# @package _global_
# Reverse shear: min_vic + anchor + LDAD (localizes the 0.75 -> 0.90 gap).
run_name: disc_core5
model:
  chunk_target: frozen
  value_detach: false
objective:
  rollout: {weight: 0.0}
  hierarchy: {weight: 0.0}
  chunk_pred: {weight: 2.0}
  value: {weight: 0.0}
  ranking: {weight: 1.0}
data:
  n_alt: 2
