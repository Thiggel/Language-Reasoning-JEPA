# @package _global_
# Counterfactual K=2 at high unique-problem diversity and matched updates.
defaults: [edit_token_faithful_cf_k2, _self_]
run_name: edit_token_faithful_cf_k2_data_18k_once
data:
  train_size: 18000
  fresh_per_epoch: false
train:
  epochs: 1
