# @package _global_
defaults: [edit_multiscale_base, _self_]
run_name: edit_multiscale_sentence
model:
  variant: sentence
objective:
  token_aligned_pred: {weight: 0.0}
  sentence_level: {weight: 1.0, changed_weight: 1.0, unchanged_weight: 0.1}
