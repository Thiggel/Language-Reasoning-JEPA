# @package _global_
# Medium unique-trajectory anchor; same three-pass protocol as the base.
defaults: [edit_token_faithful_base, _self_]
run_name: edit_token_faithful_data_2k
data:
  train_size: 2000
  fresh_per_epoch: false
