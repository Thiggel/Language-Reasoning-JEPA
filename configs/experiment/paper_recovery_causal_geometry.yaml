# @package _global_
# Matched recovery audit: causal predictor under precisely the same objective
# and sampled H=2 geometry teacher as paper_recovery_mlp_geometry.
defaults: [paper_causal_geometry_gar_no_prior, _self_]
run_name: paper_recovery_causal_geometry
model:
  predictor_kind: causal
  macro_k: 0
data:
  geo_rank_horizon: 2
  geo_rank_policy: random
