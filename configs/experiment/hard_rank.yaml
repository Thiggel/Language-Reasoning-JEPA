# @package _global_
# Hard-domain baseline for the conditioning test: core5-style ranking, 9M.
defaults:
  - override /data: igsm_hard
run_name: hard_rank
model:
  chunk_target: frozen
  value_detach: false
objective:
  rollout: {weight: 0.0}
  hierarchy: {weight: 0.0}
  chunk_pred: {weight: 2.0}
  value: {weight: 0.0}
  ranking: {weight: 1.0}
data:
  n_alt: 2
