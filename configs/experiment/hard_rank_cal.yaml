# @package _global_
defaults:
  - override /data: igsm_hard
# Hard domain: sheared recipe + cost ranking — is depth calibration the
# rescue for the catastrophic look-2 collapse (0.220 -> 0.010)?
run_name: hard_rank_cal
model:
  chunk_target: frozen
  value_detach: false
objective:
  rollout: {weight: 0.0}
  hierarchy: {weight: 0.0}
  chunk_pred: {weight: 2.0}
  value: {weight: 0.0}
  ranking: {weight: 1.0}
  cost_ranking: {weight: 1.0}
data:
  n_alt: 2
