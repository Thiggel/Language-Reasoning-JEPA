# @package _global_
# Both geometry regularizers with the milder straightening weight:
# straighten 0.02 + monotone 1.0 on the combo recipe. Likely the best
# combination if lambda=0.1 straightening proves too destructive.
run_name: disc_geo_lo
model:
  chunk_target: frozen
  value_detach: false
objective:
  chunk_pred: {weight: 2.0}
  straighten: {weight: 0.02}
  monotone: {weight: 1.0}
