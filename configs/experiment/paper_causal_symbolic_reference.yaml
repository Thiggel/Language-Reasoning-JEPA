# @package _global_
# Annotated ceiling: exact remaining-work comparisons replace latent-goal
# preference labels, while architecture and non-ranking objectives match J3.
defaults: [paper_causal_j2, _self_]
run_name: paper_causal_symbolic_reference
objective:
  ranking: {weight: 1.0, margin: 0.5}
data:
  n_alt: 2

