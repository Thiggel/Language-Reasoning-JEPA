# @package _global_
# LDAD x VICReg x residual grid cell.
run_name: disc_nodelta_novic
model:
  chunk_target: frozen
  value_detach: false

objective:
  chunk_pred: {weight: 2.0}
  delta_action: {weight: 0.0}
  vicreg: {weight: 0.0}
