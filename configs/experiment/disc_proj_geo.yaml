# @package _global_
# Both geometry regularizers on the learned projection pi(s).
run_name: disc_proj_geo
model:
  chunk_target: frozen
  value_detach: false
  geo_proj: true
objective:
  chunk_pred: {weight: 2.0}
  straighten: {weight: 0.1}
  monotone: {weight: 1.0}
