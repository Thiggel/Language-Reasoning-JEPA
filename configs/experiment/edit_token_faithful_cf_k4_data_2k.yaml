# @package _global_
# Four exact unlabeled alternatives per state at the medium data anchor.
defaults: [edit_token_faithful_cf_k4, _self_]
run_name: edit_token_faithful_cf_k4_data_2k
data:
  train_size: 2000
  fresh_per_epoch: false
train:
  batch_size: 8
