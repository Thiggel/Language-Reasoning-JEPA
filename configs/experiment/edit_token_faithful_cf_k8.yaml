# @package _global_
# Saturation/compute endpoint for exact mechanical counterfactual coverage.
defaults: [edit_token_faithful_base, _self_]
run_name: edit_token_faithful_cf_k8
data:
  counterfactual_k: 8
  counterfactual_source: uniform_local
objective:
  counterfactual_outcome: {weight: 1.0}
train:
  batch_size: 4
