# @package _global_
# Hierarchy ablation on the reference recipe: no macro-action/F_hi loss.
# Every other run trains it at weight 0.5 — does it shape the geometry
# (traj_cos, goal_dist_corr) or planning at all?
run_name: disc_combo_nohier
model:
  chunk_target: frozen
  value_detach: false
objective:
  chunk_pred: {weight: 2.0}
  hierarchy: {weight: 0.0}
