# @package _global_
# Faithful iGSM: is 0.245 undertraining? 60 epochs.
defaults:
  - override /data: igsm_real
run_name: real_combo_long
model:
  chunk_target: frozen
  value_detach: false
  max_chunk_len: 96
objective:
  chunk_pred: {weight: 2.0}
train:
  epochs: 60
