# @package _global_
# Scale-up wave 1: the final recipe on the HARD domain (14-24 vars,
# 10-18 necessary steps — squarely in the current failure zone).
defaults:
  - override /data: igsm_hard
run_name: hard_mdr_cal
model:
  chunk_target: frozen
  value_detach: false
objective:
  chunk_pred: {weight: 2.0}
  monotone: {weight: 2.0, margin: 0.05}
  value: {weight: 0.0}
  value_distill: {weight: 1.0}
  ranking: {weight: 1.0}
  cost_ranking: {weight: 1.0}
data:
  n_alt: 2
