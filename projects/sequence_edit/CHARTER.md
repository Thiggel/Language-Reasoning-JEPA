# Charter

Test whether a joint-embedding model can represent and plan literal insert, delete, and replace operations over a mutable reasoning sentence without symbolic defect rankings or oracle feasible-edit menus.
