# Sequence-edit status

The faithful token-edit implementation and hierarchy pilot are implemented. The first full-length jobs failed before optimization because the positional table was too short; that implementation defect is fixed and tested. A corrected round exists as a reviewed plan but was not submitted because the global weekly GPU-hour guard fired. No new experiment is admitted during this migration.
