# Token-level iGSM artifact map

- Scientific memory and historical waves: `research/hard_text/`.
- Current cycles: `research/cycles/hard_text/`.
- Configurations: `configs/experiment/hard_*`, `text_hier_*`, and `deltajepa_text_*`.
- Runs: existing `runs/` paths and project-qualified future runs under `runs/autonomy/token_igsm/`.
- Reports: `research/reports/token_igsm/`.

Existing paths are append-only and are not moved by the autonomy migration.
