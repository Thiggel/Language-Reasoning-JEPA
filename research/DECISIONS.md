# Decision ledger

| Date | Decision | Evidence | Revisit when |
|---|---|---|---|
| 2026-07-16 | Use fresh Codex contexts with compact repository memory and a deterministic external controller. | Long jobs and remote filesystems outlive a model context; the checkout already contains historical context overflow. | Codex provides an authoritative reservation/retrieval control plane for all sites. |
| 2026-07-16 | Keep automatic submission disabled during bring-up. | Main checkout is dirty; no backend has passed the new submit–poll–retrieve contract. | One smoke test succeeds on each enabled backend and failure/retrieval paths are tested. |
| 2026-07-16 | Treat observed intent phrases, token-level causal iGSM, and sequence editing as three separate subprojects with shared infrastructure. | Their action interfaces, deployment semantics, and defensible claims differ materially. | Only through an explicit cross-project transfer experiment. |
| 2026-07-16 | Exclude hierarchy from the paper-facing intent-phrase recipe. | Corrected continuous and discrete hierarchical planners fail matched flat confirmation; earlier gains contained ordering artifacts. | A new information-matched hierarchy beats the flat controller on a predeclared fresh confirmation. |
| 2026-07-16 | Diagnose the causal intent-phrase selection gap before broad component or scale sweeps. | Current causal J3 reaches .588 strict versus .827 for the matched token policy; adding toy ablations without localizing the gap has low paper value. | The diagnostic separates dynamics, teacher quality, and student calibration and identifies a viable mechanism. |
