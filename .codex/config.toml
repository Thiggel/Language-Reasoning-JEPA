# Interactive project defaults. The outer controller repeats these explicitly
# for unattended oversight so user-global defaults cannot silently change it.
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

